import {TQuestionsList} from "@/lib/types";


interface IQuestionList {
    questions: TQuestionsList
}
export default function AnswerSection ({ params }: { params: { slug: '123', questions: IQuestionList, } }) {

}
